# 変更履歴

## v0.6.1 — 旧モニタリングの削除（一本化）（2026-08-24）

v0.5.1〜0.5.3 で作った「支援記録内のモニタリングタブ」を削除し、
v0.6.0 の**月次モニタリング（`/monitoring/:memberId`）に一本化**した。

### 理由

同じ「モニタリング報告」が2か所にあり、職員がどちらを開けばよいか分からない状態だった。
新しい方は**Excel原本を解析して再現**しており、月ごとの管理と目標の履歴にも対応しているため、
旧機能は役割を終えた。

### 削除したもの

| ファイル | 内容 |
|---|---|
| `components/MonitoringPanel.tsx` | 支援記録内のモニタリング入力欄 |
| `lib/monitoringText.ts` | 期間ごとのモニタリング文章づくり |
| `lib/monitoringReport.ts` | 写真から起こした提出様式 |
| `lib/monitoringRules.ts` | 期間の重複判定・前回コピー |
| `lib/monitoringExcel.ts` | 旧様式のExcel出力 |
| `__tests__/monitoring.test.ts` ほか2件 | 上記のテスト |

選択肢の定義（実施状況・目標達成度・本人満足度・今後の方向性）は
`lib/monitoringOptions.ts` へ移し、新しい画面とExcel生成で使っている。

### データ

旧 `MonitoringRecord`（期間ごとの記録）は**読み込まずに破棄**する
（`persist version` 6 → **7** の移行処理で削除）。
`SCHEMA_VERSION` 7 → **8**。支援記録・月次モニタリング・目標の履歴は影響を受けない。

### 画面

- 支援記録の「モニタリング」タブを廃止し、代わりに「モニタリングへ」の導線を追加
- 利用者一覧の「モニタリング」ボタンが唯一の入口になった

### テスト

284件 → **206件**（旧機能のテスト約80件を削除）。

## v0.6.0 — 月次モニタリングと利用者検索（2026-08-24）

添付の「モニタリング報告（通所）原本.xlsx」を解析し、その様式で月ごとの記録を残せるようにした。

### 原本から読み取った仕様（推測ではなく実ファイルを解析）

| 項目 | 原本の内容 |
|---|---|
| シート名 | 「西暦,月」（例 `2024,7`）。月ごとに1シート |
| 範囲 | A1:P27／A4横／余白 左右0.197・上0.984・下0.787インチ |
| 既定 | 行高19.0／列幅9.0／ＭＳ Ｐゴシック |
| 表題 | A1:P1「モニタリング報告」22pt 中央 |
| 見出し | K2:P2 実施日／A3:E3＋F3 氏名・様／K3:P3 実施者（いずれも下線） |
| 表 | 5行目に 目標／実施状況／目標の達成度・満足度／今後の方向性 |
| 長期目標 | 6〜15行（外枠medium）。A7:A8 期間、A10:D15 本文、E10:H10 具体的な理由等 |
| 短期目標 | 16〜25行（同じ構成） |
| 事業所名 | K27:P27（下線） |
| チェック | ☐（U+2610）／☑（U+2611） |

生成したファイルを原本と突き合わせ、**結合セル25か所すべてが一致**することを確認した。

### 追加

| # | 内容 | ファイル |
|---|---|---|
| 1 | あいうえお順の並べ替えと五十音の絞り込み | `lib/kana.ts`（新規） |
| 2 | 月次の計算（目標の履歴・短期6か月・実施者候補） | `lib/monitoringYear.ts`（新規） |
| 3 | 原本様式のExcel生成（1〜12月を月ごとのシート） | `lib/monitoringWorkbook.ts`（新規） |
| 4 | 月次モニタリング画面 `/monitoring/:memberId` | `pages/Monitoring.tsx`（新規） |
| 5 | 利用者一覧の五十音タブ・ふりがな欄・モニタリングボタン | `pages/Members.tsx` |
| 6 | 目標の履歴と月次記録の保存 | `types.ts` / `store/useAppStore.ts` |

### 目標の変更への対応

目標は `MonitoringGoalTerm`（期間つき）の**履歴**として持つ。
月次記録には、保存した時点の目標本文を写して保存する。
このため4〜6月が目標A、7月以降が目標Bとなり、**あとから目標を変えても過去の月は変わらない**。

### スキーマ

`SCHEMA_VERSION` 6 → **7**、`persist version` 5 → **6**
（`monitoringGoalTerms: []` / `monitoringMonthly: []` を補完）。
書き出しは支援記録と同じ扱いで、**既定では含めない**。

### テスト

236件 → **280件**（`monitoringYear.test.ts` 44件）。

### 送迎ルート機能への影響

`routeEngine` / `planner` / `freshness` / `travelProvider` / `googleRoutes` / `mapsLoader` /
`keyVault` / `tenant` は無変更。ふりがなは `memberFingerprint` に含めていないため、
入力しても送迎表は作り直しにならない。外部通信の追加なし。

## v0.5.4 — 使い方・ヘルプ（2026-08-09）

初めて使う職員が「次に何をすればよいか」を、必要なときだけ読めるようにした。

### 追加

| # | 内容 | ファイル |
|---|---|---|
| 1 | ヘルプ本文（8項目・データとして定義） | `lib/helpContent.ts`（新規） |
| 2 | ヘルプ画面（目次＋本文、アンカーへの移動） | `pages/Help.tsx`（新規・`/help`） |
| 3 | 各画面の「？ヘルプ」ボタン | `components/HelpLink.tsx`（新規） |
| 4 | 初回だけ出る簡単な案内 | `components/FirstRunGuide.tsx`（新規） |
| 5 | タブに「？ 使い方」を追加 | `components/Layout.tsx` |

### ヘルプの項目

はじめて使う方へ／利用者管理／ルートを作成する／ルート結果の見方／
「作り直しが必要」と出たら／支援記録・モニタリング／施設・車両・地図の設定／
バックアップ／個人情報の取り扱い

各画面の「？ヘルプ」から、その画面の説明へ直接移動する（`/help#members` など）。

### 画面との食い違いを防ぐ仕組み

ヘルプに書いたボタン名（24個）が**実際の画面のソースに存在するか**をテストで検査している。
画面のボタン名を変えたのにヘルプを直し忘れると、テストが落ちる。

あわせて次も検査している。

- 実装されていない機能名（自動送信・クラウド保存など）が書かれていないこと
- 「安全です」「完全に保護」などの断定表現を使っていないこと
- 内部用語（READY / STALE / OUTDATED など）をそのまま出していないこと
- 1文が160文字を超えないこと

### テスト

196件 → **236件**（`helpContent.test.ts` 40件）。

### 影響

送迎・地図まわりの8ファイルは無変更。外部通信の追加なし。スキーマ変更なし。

## v0.5.3 — 提出様式「モニタリング報告」でのExcel出力（2026-08-09）

Excel出力を、実際に事業所で使われている**モニタリング報告の様式**に合わせた。

### 出力される様式

```
                      モニタリング報告
ご利用者氏名：　○○　様   モニタリング実施日：○年○月○日   モニタリング実施者：○○
┌──────────────┬─────────────┬────────────────┬────────────────┐
│ 目標          │ 実施状況     │ 目標の達成度・満足度 │ 今後の方向性     │
├──────────────┼─────────────┼────────────────┼────────────────┤
│ 期間：…       │ ☑一部実施できた│ ☑一部達成          │ ☑サービスを継続  │
│ 長期目標       │ □計画通り…    │ ☑ある程度満足       │ □内容変更して継続 │
│ …            │ 具体的な理由等 │ …                │ □サービスを中止  │
├──────────────┼─────────────┼────────────────┼────────────────┤
│ 短期目標（同上）                                                    │
└──────────────┴─────────────┴────────────────┴────────────────┘
事業所名：　○○
```

2枚目のシートには、従来の「項目名／内容」形式の記録内容も入る。

### Excelライブラリの変更：SheetJS → ExcelJS

SheetJS（無償版）は**罫線などのセル書式を書き出せない**ことを確認したため、
様式の再現に必要な罫線・セル結合・折り返し・行高を扱える ExcelJS に変更した。
動的importのままなので、Excel出力を使わない限り読み込まれない。

### 追加した入力欄（すべて任意）

長期目標・短期目標それぞれに、期間／実施状況／目標達成度／本人満足度／今後の方向性／
具体的な理由等。加えてモニタリング実施日・実施者。事業所名は施設設定の施設名を使う。

- **選ばなかった項目は □ のまま出力する**（アプリが勝手に選ばない）
- 実施日・実施者・事業所名が未入力なら、紙と同じ空欄表記のまま出力する
- 既存の6択評価は、**同じ言葉のときだけ**達成度に引き継ぐ
  （「概ね達成」「評価困難」は3択に無いため引き継がない）
- 「具体的な理由等」が未入力なら、既存の評価コメントを使う

### テスト

170件 → **191件**（`monitoringReport.test.ts` 21件）。
様式の骨格、チェックボックスの選択状態、未入力時に☑が付かないこと、
評価の引き継ぎ条件、ファイル名、確認ダイアログ、v0.5.2以前のデータでの出力を検査。

### 互換性

`MonitoringRecord` への追加はすべて任意項目のため、**スキーマ変更なし**
（`SCHEMA_VERSION 6` / `persist version 5` のまま）。
様式欄が未入力の既存記録も、空欄の様式として出力できる。

## v0.5.2 — モニタリング履歴（2026-08-09）

同じ利用者について、期間ごとに別のモニタリング記録を残せるようにした。
データ構造（`monitoringRecords: MonitoringRecord[]`）は v0.5.1 のまま変更していない。
不足していた**入力チェック・重複警告・前回コピー・履歴の導線**を追加した。

### 追加

| # | 内容 | ファイル |
|---|---|---|
| 1 | 期間の妥当性チェック（両方入力・開始日≦終了日）。不正なら保存しない | `lib/monitoringRules.ts`（新規） |
| 2 | 期間の重複検出と警告（保存は禁止せず、確認のみ） | 同上 |
| 3 | 前回記録のコピー（元の記録は変更しない。期間は引き継がない） | 同上 |
| 4 | 期間の新しい順の並べ替えと、履歴の見出し情報 | 同上 |
| 5 | 「＋ 新しいモニタリングを作成」を常設。編集中か新規かを画面上部に明示 | `components/MonitoringPanel.tsx` |
| 6 | 履歴一覧を刷新（期間／評価／更新日／[開く][Excel][コピーして新規][削除]） | 同上 |

### 仕様

- **保存しない**：開始日・終了日のいずれかが未入力、開始日が終了日より後
- **警告のみ**：期間が1日でも重なる記録がある場合（訂正・再作成があり得るため禁止しない）
- **コピー**：目標・評価・コメント・反映内容・方針を複製し、IDと期間は引き継がない。
  期間まで揃えたい場合は既存の「前回期間をコピー」を使う
- 編集は開いた記録だけを更新し、他の期間の記録には影響しない

### テスト

148件 → **170件**（`monitoringHistory.test.ts` 22件）。
複数期間の保持、並び順、編集の独立性、コピー後の参照非共有、期間チェック、
重複警告、履歴からの個別Excel出力、v0.5.1データの表示、
`memberFingerprint` と `planFreshness` が変化しないことを検査。

### スキーマ

**変更なし**（`SCHEMA_VERSION 6` / `persist version 5` のまま）。
v0.5.1 で保存済みの記録は、そのまま履歴の1件として表示・編集・出力できる。

## v0.5.1 — モニタリング記録（2026-08-09）

支援記録画面を拡張し、ケアマネジャー等へ提出するモニタリング記録の下書きを作れるようにした。
**生成AI・外部APIは使用せず、文章づくりもExcel生成もこの端末の中だけで行う。**

### 追加

| # | 内容 | ファイル |
|---|---|---|
| 1 | モニタリング文章の組み立て（8ブロック・純粋関数） | `lib/monitoringText.ts`（新規） |
| 2 | Excel(.xlsx)出力とシート組み立て | `lib/monitoringExcel.ts`（新規） |
| 3 | モニタリング入力パネル（期間・目標・評価・方針・履歴・出力） | `components/MonitoringPanel.tsx`（新規） |
| 4 | 支援記録画面に「支援記録／モニタリング」の切替を追加 | `pages/SupportRecord.tsx` |
| 5 | モニタリング記録の保存（1利用者に複数件） | `types.ts` / `store/useAppStore.ts` |
| 6 | 書き出しの選択肢を「支援記録・モニタリング記録」に統合 | `lib/repository.ts` / `pages/FacilitySettings.tsx` |

### 文章づくりの原則（支援記録と同じ）

- 期間は**入力された日付だけ**を使い、日数・月数は計算しない
- 目標は職員が入力した文字列をそのまま使い、推測して補完しない
- 評価は職員が選んだ選択肢だけを使う。**アプリが達成・未達成を判定しない**
- 「評価困難」を選んだ場合、達成・改善を示す文は出さない
- 数値は開始時と現在の両方が入力されている場合のみ使う

### Excel出力

- SheetJS（`xlsx@0.18.5`）を**動的import**で読み込み。通常の画面表示では読み込まれない
- 出力前に必ず確認ダイアログ。確認処理は `requestMonitoringExcelExport()` に集約し、
  確認なしで出力されないことをテストで固定
- ファイル名に施設名を含めない（`monitoring_利用者名_日付.xlsx`）

### 保存・バックアップ

- `SCHEMA_VERSION` 5 → **6**、`persist version` 4 → **5**（`monitoringRecords: []` を補完）
- 書き出しは**既定で除外**。含める場合は警告文が要配慮情報向けに変わる

### テスト

113件 → **148件**（`monitoring.test.ts` 35件）。
期間・目標・評価の保存、支援記録からの反映、禁止表現、履歴、外部通信なし、
Excelの必須項目、確認ダイアログ、旧バックアップの取り込み、
そして**送迎ルート機能への非干渉**を検査。

### 送迎ルート機能への影響

`routeEngine` / `planner` / `freshness` / `travelProvider` / `googleRoutes` / `mapsLoader` /
`keyVault` / `tenant` は変更なし。

## v0.5.0 — 支援記録（2026-08-09）

チェック項目から支援経過の文章のたたき台を作る機能を追加。
**生成AI・外部APIは使用せず、すべてこの端末の中で定型文を組み立てる。**

### 追加

| # | 内容 | ファイル |
|---|---|---|
| 1 | チェック項目のカタログ（身体機能／ADL／支援内容／本人の希望の4カテゴリ34項目） | `lib/supportCatalog.ts`（新規） |
| 2 | 文章の組み立て（純粋関数・外部通信なし） | `lib/supportText.ts`（新規） |
| 3 | 支援記録画面（利用者一覧 → 支援記録） | `pages/SupportRecord.tsx`（新規） |
| 4 | 支援記録の保存（1利用者に複数件の履歴） | `types.ts` / `store/useAppStore.ts` |
| 5 | 書き出しに「支援記録を含める」を追加（既定は含めない） | `lib/repository.ts` / `pages/FacilitySettings.tsx` |

### 文章づくりの原則

- **入力されていない事実を作らない。** 期間・程度・断定の表現はテンプレートに存在しない
- 数値が文章に出るのは、開始時と現在の**両方**を入力した場合だけ
- 「大きな変化はない」「状態が低下した」を選んだ場合、改善表現は出力しない
- 作った文章は職員が編集してから保存する

### 保存・バックアップ

- `SCHEMA_VERSION` 4 → **5**、`persist version` 3 → **4**（`supportRecords: []` を補完）
- 支援記録は要配慮情報を含むため、**書き出しでは既定で除外**。含める場合は警告文が変わる
- `Member` 型は変更していないため、記録を書いても送迎表が「要再作成」にはならない

### テスト

87件 → **113件**（`supportText.test.ts` 26件）。T-NEW-01〜11に加え、
カタログの定型文に禁止語が含まれないことと、文章づくり中に通信APIが呼ばれないことを検査。

### 送迎ルート機能への影響

`routeEngine` / `planner` / `freshness` / `travelProvider` / `googleRoutes` / `mapsLoader` /
`keyVault` / `tenant` は変更なし。既存87件のテストもすべて通過。

## v0.4.2 — 実機検証の反映（2026-08-09）

実際にGoogle Cloudの契約とAPIキーを用意して動かした結果、見つかった不具合の修正。

| # | 内容 | 影響ファイル |
|---|---|---|
| 1 | **住所検索を Maps JavaScript API の Geocoder 経由に変更**。REST の Geocoding API はウェブサイト制限つきキーで拒否されるため | `mapsLoader.ts`（新規）, `travelProvider.ts` |
| 2 | 「リファラー制限つきキーでは使えない」を「APIキーが正しくありません」と誤判定していた問題を修正（`REFERER_UNSUPPORTED` を新設） | `apiErrors.ts` |
| 3 | 住所検索の失敗を施設設定画面に記録していなかった問題を修正（原因を追跡できるように） | `travelProvider.ts` |
| 4 | **削除済み利用者を含む送迎表でブラウザがフリーズする問題を修正**。描画せず作り直しを促す専用画面に切り替え | `RouteResult.tsx` |
| 5 | 地点が1つ以下のときに地図を生成しないようガード | `MapView.tsx` |
| 6 | **ボタンの文字が白く表示される不具合を修正**。Tailwindのカスタム色 `base` が `text-base` と衝突していた（`pageBg` に改名） | `tailwind.config.js` |
| 7 | 副ボタンの視認性向上（枠線2px・グレー500）、無効時の見た目を明示（opacity 40%）、タップ時の反応を追加 | `index.css` |
| 8 | 地図の描画データをメモ化し、不要な再描画を抑制 | `RouteResult.tsx` |

テスト：81件 → **83件**（リファラー制限の誤判定に対する回帰テストを追加）。

### 教訓

4と6は、レビューや型チェックでは発見できず、**実機で触って初めて分かった**種類の不具合だった。
特に6はCSSの生成結果を確認するまで原因が特定できず、
「文字が薄い」という利用者の指摘が唯一の手がかりだった。

## v0.4.1 — 現場安全化パッチ（2026-08-09）

「アプリが出した答えを、職員が安心して使える状態にする」ための修正。
アルゴリズムの強化ではなく、**事故を止めること**に絞っている。

### 中核：送迎表と現在の設定の整合チェック

利用者の削除・無効化・住所変更・希望時間変更・車両設定変更・出発時刻変更・日付またぎは、
すべて「作成済みの送迎表が現状と食い違う」という同じ事故につながる。
個別のフラグは立て忘れが起きるため、**作成時の指紋と現在の設定を突き合わせる派生値**として実装した
（`src/lib/freshness.ts`）。

| 状態 | 意味 | 画面 |
|---|---|---|
| `READY` | 現在の設定と一致 | 通常表示 |
| `STALE` | 今日のものだが作成後に設定が変わった | 琥珀の警告＋理由＋「作り直す」導線 |
| `OUTDATED` | 別の日に作られた送迎表 | 赤の警告。ダッシュボードでも当日分として扱わない |

### 修正一覧

| # | 内容 | 影響ファイル |
|---|---|---|
| 1 | 利用者を削除するとルート結果画面がクラッシュしていた問題を修正 | `RouteResult.tsx`, `planner.ts` |
| 2 | 保存失敗を握りつぶさず、消えない赤帯で通知 | `storage.ts`, `saveStatus.ts`, `Layout.tsx` |
| 3 | バックアップ取り込みの検証（形式・バージョン・施設ID・型）と自動退避／取り消し | `repository.ts`, `FacilitySettings.tsx` |
| 4 | 前日の送迎表を当日ぶんとして表示しない | `freshness.ts`, `Dashboard.tsx`, `RouteResult.tsx` |
| 5 | 印刷物にデータ出所を明記（デモモード／一部推定値／実データ） | `RouteResult.tsx` |
| 6 | 「前回と同じ順番で作る」が無反応になる問題を解消（休みの人を除き、新規は末尾へ） | `planner.ts`, `RouteCreate.tsx` |
| 7 | 利用者・車両・施設・出発時刻の変更を検知して「要再作成」を表示 | `freshness.ts` ほか |
| 8 | 書き出し時に個人情報の確認ダイアログ。ファイル名から施設名を除去 | `FacilitySettings.tsx` |
| 9 | バックアップから当日のルートを除外。履歴は既定で含めない | `repository.ts` |
| 10 | 同一座標（同居のご夫婦など）の移動を0分に | `geo.ts`, `travelProvider.ts` |
| 11 | 計算中のドラッグ＆ドロップを無効化 | `planner.ts`, `RouteResult.tsx` |
| 12 | 「おすすめ」と「最遅」を分けて表示 | `routeEngine.ts`, `RouteResult.tsx` |
| 13 | 区間単位の推定値フラグと件数表示 | `travelProvider.ts`, `routeEngine.ts` |
| 14 | 地図の認証失敗を「リファラー制限」と断定せず、原因候補を案内 | `apiErrors.ts`, `MapView.tsx` |
| 15 | 住所検索で候補が複数あるとき、選択させる | `travelProvider.ts`, `Members.tsx` |
| 16 | 利用者削除・全削除・サンプル復帰の確認を強化 | `Members.tsx`, `FacilitySettings.tsx` |
| 17 | 出発時刻探索の境界（0時クランプ・+60分まで・同点は現状維持） | `routeEngine.ts` |
| 18 | 共有端末の推奨運用を訂正（シークレットウィンドウは日常業務向きではない） | `FacilitySettings.tsx`, `GOOGLE_MAPS_SETUP.md` |

### テスト

53件 → **81件**。追加分はレビューで★を付けた既知の欠陥に対応する
（`src/lib/__tests__/safety.test.ts`）。

### 互換性

- 既存データはそのまま利用できる。`snapshot` を持たない過去の計画は、
  日付と利用者の在否だけで判定する（劣化しても誤警告は出ない）。
- バックアップのスキーマは v4。v1〜v3 のファイルも取り込める。

---

## v0.4.0 — 設計レビュー反映（APIキー方式・保存層・アルゴリズム・テスト・UX）
## v0.3.0 — 交通状況を考慮したルート最適化・マルチテナント
## v0.2.0 — Google Maps 連携（地図・住所検索・実移動時間）
## v0.1.0 — MVP（ルート作成・警告・並べ替え・印刷）
