# Google Maps 連携 かんたん設定ガイド

初めての方向けに、画面の順番どおりに書いています。所要時間はおよそ15分です。
**このアプリはAPIキーが無くてもデモモードで動きます。**必要になったときに設定してください。

> **原則：APIキーは施設ご自身のGoogle Cloudプロジェクトで作成してください。**
> 地図・移動時間の利用料はそのプロジェクトに請求されます。
> `.env` の共通キーは開発・体験用であり、実際の送迎業務では使わない想定です。

---

## 0. 全体の流れ

```
Googleアカウント → Google Cloud でプロジェクト作成 → 請求先アカウント登録
   → API を3つ有効化 → APIキーを作成 → キーに制限をかける
   → .env に貼り付け → npm run dev で確認
```

---

## 1. Google Cloud のプロジェクトを作る

1. https://console.cloud.google.com/ を開き、Googleアカウントでログイン
2. 画面上部のプロジェクト選択（「プロジェクトを選択」）→ **「新しいプロジェクト」**
3. プロジェクト名に `dayservice-route` などを入力 →「作成」
4. 作成後、画面上部でそのプロジェクトが選ばれていることを確認

## 2. 請求先アカウントを登録する

Google Maps Platform はクレジットカード登録が必須です（無料枠の範囲なら課金されません）。

1. 左上メニュー（≡）→ **「お支払い」**
2. 「請求先アカウントをリンク」→ 画面の案内に沿ってカード情報を登録

> 使いすぎ防止のため、**「お支払い」→「予算とアラート」** で
> 「月1,000円を超えたらメール通知」などの予算アラートを作っておくと安心です。

## 3. 必要なAPIを3つだけ有効にする

左上メニュー（≡）→ **「APIとサービス」→「ライブラリ」** を開き、
検索して次の3つを順に **「有効にする」** をクリックします。

| API名 | このアプリでの用途 |
|---|---|
| **Maps JavaScript API** | ルート結果画面の地図表示 |
| **Geocoding API** | 利用者登録での「住所 → 緯度経度」 |
| **Routes API** | 実際の道路の移動時間・距離・ルート形状・**出発時刻の交通状況** |

> これ以外（Places API、Directions API など）は使っていないので有効化不要です。
> 有効化の反映に数分かかることがあります。

## 4. APIキーを作る

1. 左上メニュー（≡）→ **「APIとサービス」→「認証情報」**
2. 上部の **「＋ 認証情報を作成」→「APIキー」**
3. 表示された長い文字列（`AIza...`）を **コピー** します

## 5. APIキーに制限をかける（重要）

作成直後のキーは誰でも使えてしまうため、必ず制限します。
「認証情報」画面で作ったキーの名前をクリックし、次の2か所を設定します。

**① アプリケーションの制限**

- 「ウェブサイト」を選択
- 「ウェブサイトの制限」に次を追加
  - `http://localhost:5173/*` （自分のPCで試す用）
  - 公開する場合はその URL（例：`https://example.com/*`）も追加

**② API の制限**

- 「キーを制限」を選択
- リストから **Maps JavaScript API / Geocoding API / Routes API の3つだけ** にチェック

最後に「保存」。反映まで数分かかることがあります。

## 5.5 割り当て（Quota）の上限を決める（強く推奨）

キーが漏れた場合の被害を金額として止める、最後の砦です。

1. 「APIとサービス」→ 各API（Routes API など）→ **「割り当てと上限」**
2. 1日あたりのリクエスト上限を、実際の利用に合わせて設定
   （例：利用者10名で1日2回作成なら、1日100回もあれば十分です）

## 6. アプリにキーを設定する（推奨：画面から入力）

**施設ごとのキーは、アプリの「施設設定」画面から入力してください。**

1. アプリを開く →「施設設定」タブ
2. 「この施設のAPIキー」で **「この施設のキーを使う（推奨）」** を選択
3. APIキーを貼り付けて「保存して再読込」
4. 「キーの保存場所」を選ぶ
   - **このタブだけ**（既定・共有パソコン向け）… ブラウザを閉じるとキーは消えます
   - **この端末に保存する**（施設専用端末向け）… 次回から入力不要
5. 必要に応じて **Map ID** を入力（未入力ならテスト用の DEMO_MAP_ID が使われます）
6. **交通状況の利用** を選択
   - 考慮する（推奨）… Routes API の **Pro** 課金
   - 考慮しない… **Essentials** 課金で安価（渋滞の増加時間は表示されません）

### （開発・デモ用）`.env` に共通キーを書く場合

開発中や体験利用でのみ使います。プロジェクトフォルダで `.env.example` をコピーし、
`.env` にリネームして編集します。

```
copy .env.example .env
notepad .env
```

```
VITE_GOOGLE_MAPS_API_KEY=AIzaSy...（あなたのキー）
```

> ・`=` の前後にスペースを入れない
> ・引用符（" や '）で囲まない
> ・末尾に余分なスペースや改行を入れない

## 7. 動作確認

画面からキーを設定した場合はそのまま使えます。
`.env` に書いた場合は、**開発サーバを一度止めて（Ctrl + C）起動し直してください**
（`.env` は起動時にしか読み込まれません）。

```
npm run dev
```

ブラウザで http://localhost:5173/ を開き、次を確認します。

1. 画面右上のバッジが **「Google Maps 連携済み」**（緑）になっている
2. 「送迎ルート作成」→ 利用者を選ぶ →「ルートを作成」
3. 「ルート結果」画面に **Google の地図** が出て、
   **F**（施設）と **①②③**（お迎え順）のマーカー、
   **道路に沿った緑のルート線** が表示される
4. 「移動時間の内訳」に **通常時 / 現在予測 / 交通による増加** の3つが出る
5. 混雑があればルート線が **橙（やや混雑）／赤（渋滞）** に色分けされる
6. 各利用者の行に「移動 ○分／○km」と実距離が出る
7. 「利用者管理」→ 編集 → 住所を入れて **「住所から座標」** ボタンで緯度経度が入る

---

## うまくいかないときは

画面に日本語のエラーが出ます。よくある原因と対処は次のとおりです。

| 画面の表示 | 原因 | 対処 |
|---|---|---|
| APIキーが正しくありません | キーの打ち間違い・余分な空白 | `.env` を見直して再起動 |
| このAPIがGoogle Cloudで有効になっていません | 3つのAPIのどれかが未有効 | 手順3をやり直す（反映に数分） |
| APIキーの利用制限でブロックされました | リファラー制限に localhost が無い | 手順5① に `http://localhost:5173/*` を追加 |
| Google Maps の利用上限に達しました | 無料枠超過・請求先未設定 | 手順2を確認、時間をおく |
| 住所が見つかりませんでした | 住所表記が特殊 | 表記を変える／緯度経度を手入力 |
| ネットワークに接続できませんでした | オフライン等 | 接続を確認して再実行 |

**どのエラーでもアプリは止まりません。** 自動でデモモード（推定値）に切り替わり、
ヘッダーのバッジが「⚠ Google Maps 接続エラー → デモモードで継続」に変わります。
詳しい原因は **「施設設定」画面の「Google Maps 連携」** に表示されます。

---

## 複数の施設で使う場合（課金を施設ごとに分ける）

「施設設定」画面で施設を追加し、その施設を選んだ状態で
**「この施設のキーを使う（推奨）」→ APIキーを入力 → 保存して再読込** としてください。
以後、その施設の地図・移動時間の利用料は、その施設のGoogle Cloudプロジェクトに請求されます。

- 施設ごとにデータ（利用者・施設情報・作成したルート）は完全に分かれます。
- 「共通キーを使う」を選んだ施設は、`.env` のキー（運営側の課金）で動作します。

---

## 費用の目安と節約の工夫

- Google Maps Platform は **2025年3月に料金体系が変わり、従来の月＄200クレジットは廃止**されました。
  現在は **SKU（機能）ごとに毎月の無料呼び出し回数**が付与されます
  （Essentials 10,000／Pro 5,000／Enterprise 1,000 回/月。最新は
  https://mapsplatform.google.com/pricing/ でご確認ください）。
- 本アプリで交通状況を使う設定は **Compute Routes Pro**、
  交通なしの設定は **Compute Routes Essentials** で課金されます。
- このアプリは**呼び出し回数を抑える作りです。**
  - 移動時間の表（マトリクス）は施設・利用者の座標と出発時刻（15分単位）が変わらない限り
    **キャッシュ**され、手動で順番を並べ替えても再取得しません。
  - 交通情報つきの計算（`TRAFFIC_AWARE_OPTIMAL`）は通常より単価が高いため、
    地点数が多いときは自動的に `TRAFFIC_AWARE` に切り替わります。
  - 取得するフィールドを必要最小限（FieldMask）に絞っています。
  - 住所検索は「住所から座標」ボタンを押したときだけ実行されます。
- 1日1回、利用者10名程度の使い方であれば、呼び出しは1日あたり数回です。

## 個人情報の扱い

| データ | Googleへ送信 |
|---|---|
| 利用者名・備考・利用者ID | **送信しません** |
| 住所 | 「住所から座標」ボタンを押したときのみ送信 |
| 緯度・経度 | 移動時間とルート取得のため送信 |
| 地図のラベル | `F` と `①②③` のみ（氏名なし） |

外部通信は `src/lib/travelProvider.ts` と `src/lib/googleRoutes.ts` の2ファイルに
すべて集約しているため、送信内容はここだけ確認すれば監査できます。

## 共有パソコン・タブレットで使うときの注意

利用者名・住所はこの端末のブラウザ内に保存されます（サーバへは送りません）。
共有端末では次の運用をおすすめします。

- 端末にPIN／パスワードロックをかけ、離席時はロックする
- 職員ごとにOSのユーザーを分ける（ブラウザの保存領域も分かれます）
- APIキーの保存場所は既定の「このタブだけ」のままにする
- 使い終わりに「施設設定 → データの管理 → **この端末のデータを削除**」を実行する
  （必要なら事前に「データを書き出す」でバックアップを取ってください）
