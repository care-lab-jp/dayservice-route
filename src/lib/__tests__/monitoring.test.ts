/** 旧モニタリング機能は v0.6.1 で削除されました。このファイルも削除してください（git rm）。 */
import { describe, expect, it } from 'vitest';
describe('旧モニタリング機能', () => {
  it('削除済み（このテストファイルは不要です）', () => {
    expect(true).toBe(true);
  });
});
